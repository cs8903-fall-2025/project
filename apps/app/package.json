{
  "name": "app",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix",
    "preview": "$npm_execpath run build && vite preview --host 0.0.0.0",
    "test": "echo \"No tests yet\" && exit 0",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.2.3",
    "@hono/eslint-config": "1.1.1",
    "@repo/config-eslint": "*",
    "@repo/config-tailwindcss": "*",
    "@tailwindcss/vite": "^4.1.13",
    "eslint": "^9.34.0",
    "tailwindcss": "^4.1.13",
    "vite": "^6.3.5",
    "vite-ssr-components": "^0.5.1",
    "wrangler": "^4.17.0"
  }
}
